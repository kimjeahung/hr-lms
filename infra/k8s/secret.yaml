apiVersion: v1
kind: Secret
metadata:
  name: hr-lms-secret
  namespace: hr-lms
type: Opaque
stringData:
  # Change before production use.
  SPRING_DATASOURCE_USERNAME: "lms_user"
  SPRING_DATASOURCE_PASSWORD: "change-me"
